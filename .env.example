# Copy this file to `.env` and fill in your own values there.
# .env is excluded via .gitignore — never commit the real one.
#
# These aren't used yet (Days 2-11 are fully client-side, no keys needed).
# They start being read once the Narrative Engine (Days 12-13) is wired up.

# IBM watsonx.ai credentials — used by the Narrative Engine (Granite model)
WATSONX_API_KEY=
WATSONX_PROJECT_ID=
WATSONX_URL=